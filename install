#!/usr/bin/env python3
"""Dotfiles package installer with tag-based management."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent))

from installer.cli import main

if __name__ == "__main__":
    sys.exit(main())
