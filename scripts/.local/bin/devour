#!/usr/bin/env bash

# Source: devour.sh from https://github.com/salman-abedin/devour.sh

ARGS=$*
CMD="${ARGS%% -- *}"
FILE="${ARGS##* -- }"
[ "$CMD" != "$FILE" ] && SAFEFILE=$(printf '%q' "$FILE")
WID=$(xdo id)

xdo hide
"${SHELL:-/bin/sh}" -i -c "$CMD $SAFEFILE" > /dev/null 2>&1
xdo show "$WID"
